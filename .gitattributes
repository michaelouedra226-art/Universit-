# Auto detect text files and perform LF normalization
* text=auto

# Ensure gradlew has LF line endings on Unix/Linux
gradlew text eol=lf

# Windows batch files must keep CRLF
*.bat text eol=crlf

# Binary files
*.jar binary
*.jks binary
*.keystore binary
